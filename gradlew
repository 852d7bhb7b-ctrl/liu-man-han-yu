#!/bin/sh
# 简易 gradlew - 使用 Android Studio 环境触发构建
export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
echo "请在 Android Studio 中打开此项目并等待 Gradle Sync 完成"
echo "然后使用 Build → Build Bundle(s) / APK(s) → Build APK(s)"
